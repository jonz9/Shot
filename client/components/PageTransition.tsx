import React from "react";
import { staggerContainer } from "@/utils/motion";

const PageTransition = () => {
  return <div className="relative overflow-hidden flex"></div>;
};

export default PageTransition;
